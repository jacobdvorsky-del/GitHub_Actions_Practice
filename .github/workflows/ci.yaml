name: CI - Pull Request
on:
  pull_request:
    branches: ['main']
permissions:
  contents: read
  
jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: echo "${{ github.ref }}"

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - run: echo "Test Job"
    
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build Job"
